# Local run logs and session notes
full_run_log.txt
# NEXT_PROMPT.md is committed as the kickoff plan for the next session
# (remove from ignore to allow tracking)
*.log
logs/*.txt

# MATLAB temporary/autosave files
*.asv
*.m~

# OS/editor junk
.DS_Store
.idea/
.vscode/

# Outputs: keep folder structure, ignore artifacts
/outputs/*
!/outputs/.gitkeep
/outputs/performance/*
!/outputs/performance/.gitkeep
/outputs/runs/*
!/outputs/runs/.gitkeep

# Session planning and references (not tracked)
NEXT_PROMPT.md
refs/
